# 第二周 · 干中学执行表

> 用法：**每天只做当天那一格。** 做完把产出贴进打卡区，再关窗口。
> 唯一验收标准：**跑得出命令、看得见输出。** 看完教程说"我理解了"不算。
> **本周配比：B 线 4 天（产出物一 `min-agent-loop`）+ A 线 3 天（PyTorch → 手撕网络 → Excel 手算注意力）。**

## 本周从哪来（一句话对齐）

那份路线图的「第 1 周」其实是**两条并列的 7 天线**：工程线 = 你上周已经做完（而且手写加强版），算法线 = 本周 A 线那 3 天。所以本周不是"补课"，是**接着走它该走的那条线**。

它的**教程链接**全部按天填进了下面各天的「卡住时才去查」——这是我们最缺的东西（原预告只说"做什么"，没说"用什么"）。

⚠️ 它里面的 **LangChain / LCEL、FAISS / Chroma、Docker、MOE 本周不做**。前两个撞 `CODEBUDDY.md` 的"手写阶段禁框架 + 先用 numpy 手算"，Docker 顺延到 B4 Harness，MOE 已明确砍掉。

**语言怎么分工（别当成"又要换语言"）**：B 线是**产出物**——要进仓库、要能对着面试讲，而且循环 / 终止 / 容错 / 日志**本来就是后端问题**，你的存量资产全在 Java 这边 → 用 **Java 17 + Spring Boot 4.0.7 + Gradle**，`login-demo` 那套直接复用。A 线是**认知**——搞懂模型内部在算什么，**PyTorch 只有 Python 生态**，Day 7 还专门要求用 Excel 手算、不许写代码。**不是"换语言"，是按目的选工具**；Python 的目标仍然只到"能读懂能改"，不要求你转 Python 开发。

---

## 开工前（30 分钟，清完就走）

- [ ] 清掉第一周 RAG 遗留三条 —— 坑⑤ 反向实验（问 `推荐三本 RAG 的书`，提示词改成"你可以结合自己的知识补充回答"）· 删掉 `rag.py` 里重复的 `EMBED_URL` · `day1.py` 问题改 `sys.argv[1]`
- [ ] 确认 JDK 17 + Gradle 可用（`login-demo` 那套现成的，跑一下 `./gradlew -v`）
- [ ] 确认 `ZHIPU_API_KEY` 能被 Java 读到（先走环境变量，别急着配置文件）

**这 30 分钟是清账，不是热身。** 做完就进 Day 1，别顺手改别的。

---

## Day 1 · 让 Java 说得上话

**今天的产出**
一个 Spring Boot 工程，跑起来能把一句话发给模型并打印回复；`Tool` 的**定义结构**成型（先只定义，不执行）。

**动手做**
1. 建 Gradle + Spring Boot 4.0.7 工程（`build.gradle` 直接参考 `login-demo`）
2. **手写 HTTP** 发 POST 调模型 —— 读环境变量里的 Key、拼 `messages`、取 `choices[0].message.content`
3. 定义 `Tool` 结构：名字 / 描述 / 需要哪些参数（**先只定义，不实现执行**）
4. 打印出"我有哪些工具可用"

> 第一天的重点不是"调通"（你 Python 里调过了），是**把 Python 那套东西翻译成 Java 的类型和分层**。

**卡住时才去查**：Spring Boot 怎么读环境变量 / `RestClient` 怎么发 POST（Spring 6.1+ 自带）/ 怎么把响应反序列化成对象
**今天别碰**：**Spring AI · LangChain4j**（`CODEBUDDY.md` 写死了）· 流式输出 · 并发 · 工具的真实执行
**验收**：终端里能看到模型回复，**并且**打印出了你定义的工具结构。
**顺带搞懂（10 分钟）**：工具的描述信息（schema）和**后端接口文档**是什么关系？

---

## Day 2 · 让它自己决定调哪个工具

**今天的产出**
一个 `while` 循环：模型说要调工具 → 代码去调 → 结果回灌 → 再问。**能跑通第一轮工具调用。**

**动手做**
1. 实现 2 个工具：`查用户信息` / `校验密码格式`（**造假数据就行，不用真连库**）
2. 把工具定义塞进请求（告诉模型有哪些工具可用）
3. 解析模型返回的"我要调 XX、参数是 YY"
4. 执行工具 → 把结果拼回对话 → 再问一次
5. **每一轮打印**：打算干什么 → 实际调了哪个、参数是什么 → 返回什么 → 下一步判断

**卡住时才去查**：模型返回的 tool_call 长什么样 / 工具结果怎么回灌进 `messages`
**今天别碰**：终止条件（明天专门做）· 重试 · 并发
**验收**：任务「查一下用户 1001 的信息，然后判断他的密码能不能通过校验」能跑通，日志看得见每一轮。
**顺带搞懂**：**「下一步干什么」是 `if-else` 决定的，还是模型决定的？** —— 这一问是产出物一的核心，答不上来等于没做。

---

## Day 3 · 让它知道什么时候该停

**今天的产出**
三层终止条件，并**故意让它坏一次**。

**动手做**
1. 第一层：模型给出最终答案 → 正常结束
2. 第二层：轮数上限（比如 10 轮）
3. 第三层：工具连续失败次数上限
4. **故意弄坏**：把某个工具改成永远抛异常，看它怎么退出、日志说了什么

**卡住时才去查**：`while` 怎么写三重退出才不乱（`break` + 标志位？还是抛异常？）
**今天别碰**：重试机制（那是 B4 Harness 的事，本周不做）
**验收**：正常任务能停；"工具永远失败"的任务**也能停**，且日志说清了为什么停。
**顺带搞懂**：这三层分别对应你后端熟悉的哪个机制？（熔断 / 超时 / 幂等 / 死循环保护）

---

## Day 4 · 加工具，看代码要不要改

**今天的产出**
验收全过 + `min-agent-loop/笔记.md`。

**动手做**
1. 把工具从 2 个**加到 10 个**，**记录你改了几行代码、改在哪**
2. 再故意弄坏一次：让模型返回一个**不存在的工具名**，看程序会怎么反应
3. 把 `题目.md` 里**4 个"要搞明白的问题"**的答案写进 `min-agent-loop/笔记.md`

**今天别碰**：加新功能。这天只收口，写代码的时间应该很短。
**验收（`题目.md` 的三条，一条不许省）**
- [ ] 能跑通一个需要**两轮以上**工具调用的任务
- [ ] 打印的日志能让我看清每一轮发生了什么，出问题能定位
- [ ] 上面四个问题都能**口头**讲清楚

**顺带搞懂**：**工具从 2 个到 10 个，代码不用改** —— 这件事说明了 Agent 和传统后端的什么本质差别？

---

## Day 5 · 先把 PyTorch 跑起来（A 线）

**今天的产出**
一个跑得起来的 PyTorch 训练循环。

**动手做**
1. 装 PyTorch（**CPU 版就够**，本周不碰 GPU）
2. 张量操作：创建 / 形状 / 矩阵乘
3. 跟一遍最小训练循环：前向 → 算损失 → 反向传播 → 更新参数

**卡住时才去查（本周的资源都在这儿）**
- `Karpathy · Neural Networks: Zero to Hero` —— **和你「自己写」的习惯最匹配**
- `3Blue1Brown · 线性代数的本质` —— 用来建直觉，**不用记公式**
- 《动手学深度学习》（B站）

**今天别碰**：调参 · GPU · 大模型（只跑玩具任务）
**验收**：循环跑起来，**`loss` 数字在下降**。
**顺带搞懂**：`loss.backward()` 到底算了什么？

---

## Day 6 · 手撕一个神经网络（A 线）

**今天的产出**
从零实现一个简单前馈网络 + 反向传播（**几十行就够**）。

**动手做**
1. 先用最笨的办法写前向（手写权重，不用 `nn.Module` 的现成层）
2. 手写（或跟着推）反向传播
3. 在一个玩具任务上训到能收敛

**卡住时才去查**：`Neural Networks from Scratch` · `Karpathy Zero to Hero`（第 1-2 集）
**今天别碰**：PyTorch 的高级封装 —— **这一天的目的是把底层走一遍**
**验收**：**能用自己的话说清"梯度是怎么传回去的"**。
**顺带搞懂**：为什么"求导"这件事能自动做？（计算图）

---

## Day 7 · 用 Excel 手算注意力（A 线）

**今天的产出**
一张 Excel 表，**手算**出 Q/K/V 和注意力分数。

**动手做**
1. 拿一个 3-4 个词的句子，手工填一组 embedding（随便填数）
2. 算出 Q、K、V（就是三组矩阵乘）
3. 走完整条：`QKᵀ` → 除以 `√d` → `softmax` → 乘 `V`
4. **每一步都用单元格公式，不许用 Python**

**卡住时才去查**：`Transformer from scratch in Excel`（**专门教这个的**）· `The Illustrated Transformer`
**今天别碰**：手撕 Multi-Head Attention（那是下周）
**验收**：你能**指着表格**说清"注意力在算什么"——每一格是什么、为什么这么乘。
**顺带搞懂（本周收口）**：A 线和 B 线是怎么接上的？——本周 4 天做的"循环 + 工具"，和上周的"切分 + 检索"，在同一张图上的什么位置？

---

## 全周统一验收标准

> **跑得出命令、看得见输出，才算过。**

- 看懂教程 ≠ 学会了
- 照抄能跑 ≠ 学会了
- **能把它故意弄坏再修回来 = 学会了**

本周两份产出：

| 产出物 | 位置 | 硬指标 |
|---|---|---|
| 产出物一 · 最小 Agent 循环 | `min-agent-loop/` | `题目.md` 三条验收全过 |
| A 线手算笔记 / Excel | `notes/` 或 `min-agent-loop/` | 能指着表说清注意力在算什么 |

---

## 打卡区

| Day | 日期 | 产出跑通了吗 | 卡在哪 | 花了多久 |
|---|---|---|---|---|
| 1 |  | ☐ |  |  |
| 2 |  | ☐ |  |  |
| 3 |  | ☐ |  |  |
| 4 |  | ☐ |  |  |
| 5 |  | ☐ |  |  |
| 6 |  | ☐ |  |  |
| 7 |  | ☐ |  |  |

---

## 第三周预告

| 线 | 做什么 |
|---|---|
| A | **手撕 Multi-Head Attention、Transformer Block** —— 把本周砍掉的那段补上，A2 收口 |
| B | 开工产出物二 `research-assistant/`：工具调用 + 检索 + 多轮记忆 + 上下文上限 |
| 顺延 | Docker 打包（并入 B4 Harness 阶段一起做） |

---

## 三条别忘的规矩

1. **每天只做当天那一格。** 提前做完就休息，不要抢第 2 天的内容。
2. **卡住先查，查不到再来问我。** 但别一卡就退回去系统地学——只查挡住你的那一个点。
3. **关窗口前先把产出贴进打卡区。** 判断标准就一句：关掉这个窗口，有什么东西留下来了？

---

## 附：本方案 ↔ AgentGuide「第 1 周」截图对照

> 🔑 它的「第 1 周」是**两条并列的 7 天线，合计 14 天**——一周装不下，所以被摊进了我们的第一周 + 第二周。**不是"没学完"，是账本来就算错了一层。**

### 工程线 7 天 → 第一周（已完成）

| 截图 | 主题 | 去向 |
|---|---|---|
| D1 | FastAPI 快速入门 | ✅ 第一周 D2（`day2.py`，最小版） |
| D2 | LangChain 核心概念 + LCEL | ⚠️ **降级不做** —— 撞「手写阶段禁框架」 |
| D3 | RAG Part1 · 加载与分割 | ✅ 第一周 D3（`day3.py`）**手写版**（它用 LlamaIndex Loaders） |
| D4 | RAG Part2 · 向量化与存储 | ✅ 第一周 D4（`day4.py`）**numpy 手算版**（它用 FAISS/Chroma） |
| D5-6 | 手撕 Naive RAG 系统 | ✅ 第一周 D5 + D6（`day5.py` / `day6.py`）—— 比它的"整合 FastAPI+LangChain"更硬 |
| D7 | 周度总结与项目部署（Docker） | ⏭️ 顺延到 B4 `harness-demo/` |

### 算法线 7 天 → 第二周 A 线（本次）+ 后面

| 截图 | 主题 | 去向 |
|---|---|---|
| D1 | Python & PyTorch 基础 | ⬜ **第二周 D5**（只取 PyTorch；Python 基础不单补，能读懂即可） |
| D2 | 手撕神经网络训练 | ⬜ **第二周 D6**（资源照搬：Networks from Scratch + Karpathy） |
| D3 | Transformer 宏观理解 | ⚠️ **拆开**：资源（Illustrated Transformer）进第二周 D7，知识目标（Encoder/Decoder + MHA）顺延第三周 |
| D4 | Transformer 矩阵计算（Excel） | ⬜ **第二周 D7**（资源照搬：`Transformer from scratch in Excel`） |
| D5 | 手撕 Multi-Head Attention | ⏭️ 第三周 A 线 |
| D6 | 手撕 Transformer 关键模块 | ⏭️ 第三周 A 线 |
| D7 | MOE 架构与模型家族 | ❌ **不做** —— 超出认知级，且撞「不碰模型训练/微调」 |

### 第二周 B 线 4 天 = 截图里没有的部分

`min-agent-loop`（Agent 循环 / 工具调用 / 终止条件）**不在它第 1 周里**。它来自 AgentGuide 更后面的 **「Agent 系统与 Harness」+「Tools 与协议生态」**，对应 `学习计划.md` 的 **B2**。

> 也就是说：第二周 = 「截图算法线的 3 天」+「截图里根本没有的 Agent 循环 4 天」。后者才是你的主线产出物。
