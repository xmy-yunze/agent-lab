# 第一周 · 干中学执行表

> 用法：**每天只做当天那一格。** 做完把产出贴进打卡区，再关窗口。
> 唯一验收标准：**跑得出命令、看得见输出。** 看完教程说"我理解了"不算。

## 开工前（只做这两件）

- [ ] 装好 Python（3.10+），能跑 `python --version`
- [ ] 拿到一个大模型 API Key，能读进环境变量

搞不定就来找我。**不要在这一步顺手去看教程。**

---

## Day 1 · 让模型开口说话

**今天的产出**
一个 Python 脚本，跑起来打印出模型的一句话。

**动手做**
1. 用 pip 装一个 HTTP 客户端
2. 读环境变量里的 API Key
3. 发一次请求给模型接口
4. 把返回的文本打印出来

**卡住时才去查**：Python 怎么读环境变量 / 怎么发 HTTP 请求 / 返回的 JSON 长什么样

**今天别碰**：FastAPI · 异步 I/O · LangChain · 任何框架

**验收**：终端里能看到模型说的一句话。**看到就算过。**

**顺带搞懂（10 分钟，别多）**：Token 是什么、这一次请求大概花了多少钱

---

## Day 2 · 把它变成一个服务

**今天的产出**
一个网址，浏览器打开能跟模型对话。

**动手做**
1. 装 FastAPI
2. 把昨天的逻辑搬进一个接口里
3. 浏览器访问，把模型的话显示出来

**卡住时才去查**：FastAPI 最小路由怎么写 / 怎么接收请求里的参数

**今天别碰**：异步 I/O（同步够用）· Pydantic 校验 · Docker

**验收**：浏览器里能看到模型回答。

**顺带搞懂**：为什么要包成服务——因为后面前端、别人、你手机都要能调它。

---

## Day 3 · 给它一份自己的资料

**今天的产出**
把一份 md 文档切成一堆小段，把每一段打印出来看。

**动手做**
1. 找一份真实的文档（越长越好，比如你的一份笔记）
2. 读进内存
3. 按某种规则切成小段
4. 打印出段数 + 每段前 50 个字

**卡住时才去查**：为什么必须切（想想上下文窗口）/ 一段太短和太长分别坏在哪

**今天别碰**：各种 Loader（PDF / 网页 / Word 都先不管，只用 txt 或 md）· 任何向量库

**验收**：能看到段数和内容，**并且你能说出"切得算不算好"**。

---

## Day 4 · 把意思变成数字

**今天的产出**
输入一个问题，从段落堆里挑出最相关的 3 段。

**动手做**
1. 调一次 embedding 接口，把每段文字变成一串数字
2. 把问题也变成一串数字
3. 用 numpy 算它们之间的相似度
4. 排序，取前 3 段，打印出来

**卡住时才去查**：文字凭什么能变成数字、那串数字代表什么 / 余弦相似度怎么算

**今天别碰**：FAISS / Chroma —— **先用 numpy 手算。** 你只有几十段，用不着库。

**验收**：你问一个问题，它挑出来的 3 段，你自己看觉得**确实相关**。

**顺带搞懂**：向量库不是"让检索更强"，它是"数据大到内存放不下时要建索引"。

---

## Day 5 · 串成一个完整的 RAG

**今天的产出**
问题进 → 答案出，**并附上它引用的原文**。

**动手做**
1. 把 Day 3 + Day 4 拼起来
2. 把检索到的 3 段拼进给模型的提示里
3. 让模型基于这些资料回答，并要求它标出用了哪几段

**卡住时才去查**：提示词怎么拼 / 上下文为什么有限

**今天别碰**：LangChain（第二周再说）

**验收（两问都要过）**
- 问一个**文档里有**的问题 → 答对，且引用对
- 问一个**文档里没有**的问题 → 它应该老实说"资料里没有"

⚠️ **如果第二问它开始瞎编——记住那个瞬间。** 这就是"幻觉"，你亲手撞上了。

---

## Day 6 · 故意把它弄坏（本周最值钱的一天）

**今天的产出**
一份「我踩过的坑」记录，每条写清：怎么坏的 → 为什么 → 怎么修。

**要故意做的四件事**
1. 问一个**必须跨好几段**才能回答的问题 → 只取 top-3 会怎样？
2. 换一份**特别长**的文档 → 切分策略还成立吗？
3. 问一个**文档里完全没提**的事 → 它编不编？
4. **同一个问题问三遍** → 答案稳不稳？

**卡住时才去查**：为什么取 top-3 不够（想想多跳问题）/ 什么叫幻觉、为什么会有

**今天别碰**：重排、多路检索 —— **知道有这回事就够了**，别现在去实现。

**验收**：至少写清 3 个坑，且**每个坑你都能讲出原因**。

> 💡 这一天产出的东西，是你以后简历上最有价值的一块。别糊弄。

---

## Day 7 · 回头看，写下来

**今天的产出**
一页纸笔记，用**你自己的话**讲清：RAG 是什么、为什么每一环都要有。

**规则**
- 不写代码
- **不查资料**，硬写
- 写不出来卡在哪里，那就是你还没懂的地方，标出来

**今天别碰**：Docker 部署（第一周不需要）

**验收**：把这页读给一个完全不懂的人，他能听明白。

---

## 全周统一验收标准

> **跑得出命令、看得见输出，才算过。**

- 看懂教程 ≠ 学会了
- 照抄能跑 ≠ 学会了
- **能把它故意弄坏再修回来 = 学会了**

---

## 打卡区

| Day | 日期 | 产出跑通了吗 | 卡在哪 | 花了多久 |
|---|---|---|---|---|
| 1 | 09-20~09-22 | ☑ | 环境变量名拼成 APL / URL 靠记忆填 / 行尾 \ 续行符 / 缩进 / 没写 else 静默失败 / 老终端里还是旧 Key |
| 2 | 09-22 | ☑ | pip 下载断线（换清华镜像）/ 冒出 .venv 导致 requests 不在里面 / uvicorn 没 cd 到 python 目录 / URL 参数名与函数参数名不一致报 422 / 忘存盘改动不生效 / 答非所问（200 但输入被忽略） | 约 3.5h |
| 3 | 09-22 | ☑ | 路径读成了脚本自己（111 字符）/ `split("##")` 连带把 `###` 也切了 / `split` 吃掉分隔符导致标题残半 / 换 `"\n## "` 后又变成段太长、主题混在一起 | 约 1.5h |
| 4 | 09-22~09-23 | ☑ | `Bearer` 后少空格报 401（报错只说"失败"不说"少空格"）/ `r["data"]` 手滑（Response 不可下标）/ `np.array(dict)` 造出 object 数组 / `ecoding` 拼错 / `def` 写在调用之后 / **检索首次失败：答案排第 6** → 对照实验定位到"长段稀释" → 改切分修复 | 跨日（09-22 夜 ~ 09-23 上午） |
| 5 | 09-23 | ☑ | `CHAT_URL` 填成 OpenAI 的地址、Key 名跟着错（智谱的模型名塞进 OpenAI 的域名）/ 第一问"答对了"其实是答非所问 —— 资料里只有"像什么"，没有"是什么" | 约 2h |
| 6 | 09-23 | ☑ | 正则层数写死（`####` 切不动，整篇 1 段）/ 短空壳段霸榜（长段稀释）/ 「资料里没有」其实是**没检索到**（两种伪装）/ **向量不辨数字**（问 A4 捞回 A2）/ 分数不可作判据（有答案 0.4424 ≈ 无答案 0.4015）/ `requests` 没写 `timeout` 导致十分钟无响应 / 引用编号会抖 / **修复不是免费的**（加前缀让长段更稀释，A4 丢了 #1）/ 改代码前没存基线 | 约 2.5h |
| 7 |  | ☐ |  |  |

### Day 1 · 09-20~09-22

**产出**：`python/day1.py` —— 终端里打印出模型的一句话。调的是智谱 `glm-4.7`。

两条最值钱的认知（比"会调 API"重要）：

- **「程序没报错」≠「跑通了」。** 没有 `else` 的 `if` 会让失败静默无声，把人往环境、网络、代理上引 —— 真正的原因被自己的代码藏起来了。
- **配置改了，已经跑着的进程不会自己变。** 环境变量在进程内存里，不在文件里；那个老终端一直拿着旧 Key。同一台机器上，两个进程读出了不同的值。

### Day 2 · 09-22

**产出**：`python/day2.py` —— `uvicorn day2:app --reload`，浏览器打开 `127.0.0.1:8000/xmy/?message=xxx` 能看到模型的回答。

- 形态：浏览器 → 我的服务 → 智谱。**这就是反向代理 / 网关的最小形态**（收请求 → 加工 → 转出去 → 拿回来 → 返回）。
- 用**查询参数**不用路径参数：路径里放不下 `/`、`?`，而"问题"里随时会有。
- 一律写 `def`，不用 `async def` —— 调的是阻塞的 `requests`，写 `async def` 会卡死事件循环。（对照：Spring MVC vs WebFlux，坑一模一样。）
- **逻辑一字没改**，变的只是「住在哪、被谁叫醒、结果交给谁」—— 这就是**控制反转**：Don't call us, we'll call you.

**今天最值钱的坑**：

- **「HTTP 200」≠「我的输入被处理了」。** 答非所问比报错难发现得多 —— 报错会大喊大叫，答非所问只是安静地回一句好听的废话。「怎么感觉不对」这个念头就是工程直觉。
- **跳过的验证步骤，会以"感觉不对"的形式回来找你。** 该分两轮跑的（先验参数、再接模型），合一轮做，就只能靠"感觉"。
- **422 的 `loc` 字段直接写着它要什么名字** —— 参数类错误先读 `loc`，别瞎猜。

**遗留**：⬜ 模型返回的 `\n` 在浏览器里显示成字面量（JSON 规范强制转义换行）。要换 `fastapi.responses` 的纯文本响应类型。不影响验收，可留到 Day 6。

### Day 3 · 09-22

**产出**：`python/day3.py` —— 把 `学习计划.md` 切成段，打印段数和每段前 50 字。

**两组对比数据（今天最值钱的产出）**：

| 切法 | 段数 | 段长 | 问题 |
|---|---|---|---|
| `split("##")` | **23 段** | 27 ~ 459 字（**17 倍**） | 标题残半、**段不自足** —— 正文跑到别的段去了 |
| `split("\n## ")` | **10 段** | 88 ~ 1730 字（**19.7 倍**） | 有大标题、自足性变好；但**长度悬殊反而变大**，最长那段 1730 字（占全文 1/3）一口气吞了 A1~A4 四个子节 |

⚠️ **注意第二行那个"反而"** —— 换切法时我的判断是"大部分能看懂"，但实测长度差距从 17 倍**涨到 19.7 倍**。**"感觉变好了"和"数据变好了"是两件事。**

> 为什么会看错：打印的是**每段前 50 字**，长度差异根本不在视野里。**输出里没有的信息，看不出问题。**

**认知**：

- **`split` 只做字符匹配，不看语义。** `"### "` 里天然含有 `"##"` —— 我以为在切二级标题，其实三级也被切了。**23 = 22 个 `##` + 1**，不是巧合。
- **`split` 会吃掉分隔符。** 所以每段开头都是"残破的标题"：H2 没了井号、H3 剩一个井号（看着反而像一级标题）。
- **切分没有"正确答案"，只有权衡。** 切小 → 精确但不自足（27 字那段只剩一句口号）；切大 → 自足但检索粗。**换一种切法，问题没消失，只是换了个形状。**
- **想按"任意级别的标题"切，`split` 做不到** —— "标题"是"1 个或多个 `#` + 空格"，那是**模式**不是**固定字符串**。得用正则（`re.split(r"\n#+ ", text)`）或逐行判断 `line.startswith("#")`。
  > ⚠️ **Day 4 修正**：`r"\n#+ "` 会把 `#` 连同空格**一起吃掉**（段首又变成"缺井号的标题"，跟 `split("##")` 同一个毛病）。正确写法要带**先行断言** → `re.split(r"\n(?=#{2,3} )", text)`。详见 Day 4 打卡详情。

**判断"切得好不好"的唯一方法**：随便挑一段，把其他段全蒙住 —— **只看它，能独立说明白一件事吗？**

**遗留**：⬜ 短段合并 / **标题路径**（把父级标题拼进段首，相当于段的 `traceId`）/ 相邻段重叠 —— 都等 Day 4 有了检索结果再决定修哪个方向。

### Day 4 · 09-22 夜 ~ 09-23

**产出**：`python/day4.py` —— 输入一个问题，从 23 段里挑出最相关的 3 段（embedding + numpy 手算余弦 + `argsort`）。

今天走的是一条**完整的工程曲线，不是顺风路**：

```
接口跑通 → 算出分数 → 检索失败（正确答案排第 6）→ 对照实验定位根因
   → 改切分 → 三个实验三中三 → 全量回归 → 打卡
```

**① 第一次检索：失败，但比成功值钱**

问「每天要花多少小时学 Agent」，正确答案（`## 一、总周期与节奏`，151 字，原文含"每天 3-4 小时"）排**第 6**，top-3 全是"文档元信息段"（导言 / 标题头 / "AgentGuide 怎么用"）。

- 「**学 Agent**」是**泛滥词** —— 几乎每段都有，连段 8「不通读 AgentGuide」都能拉分。它把查询向量拉向"整篇文档的主题"，而"文档主题"的典型代表正是标题和导言。
- 真正有区分度的「**每天几小时**」太短、太朴素，压不过它。
- 10 个分数全挤在 **0.2771 ~ 0.3994**，top-5 全距仅 **0.033** —— 正确答案与 top-1 的差距**就在噪声级别**。

**② 三组对照实验**

| 问题 | top-1 | 答案排第几 |
|---|---|---|
| 笼统版「每天要花多少小时学 Agent」 | 段1 **0.3994** | ❌ 第 **6**（0.3660） |
| 具体版「每天要学几个小时？」 | **段2 0.3697** | ✅ 第 1 |
| 「红烧肉怎么做」（完全无关） | 段6 0.1474 | — |

**③ 段 3 三战三败 → 「长段稀释」坐实**

段 3 有 **1730 字**，一口气吞了 `### A1 数学` `### A2 模型机制` `### A3 微调` `### A4 推理部署` 四个主题 —— 但**只有一个向量**。

| 实验 | 问题 | 答案藏在段3的哪块 | 段3排名 |
|---|---|---|---|
| 1 | KV Cache 是什么 | `### A4` | 第 2（0.2472） |
| 2 | 该微调还是用 RAG | `### A3` | **没进 top-3** |
| 3 | 为什么不要系统学高数 | `### A1` | **没进 top-3** |

**铁证在实验 2**：「什么时候该微调、什么时候该用 RAG？」是文档里 `### A3` 表格的**原文，一字不差** —— 照样输。**那 20 个字的信号，被同一个向量里另外 1710 个字的噪声压死了。**

**④ 改切分 → 三中三**

```python
raw = re.split(r"\n(?=#{2,3} )", text)      # 旧写法： text.split("\n## ")
```

| 问题 | 改前 | 改后 |
|---|---|---|
| KV Cache 是什么 | 段6 0.2575 ❌ | **`### A4 · 推理与部署`　0.4915** ✅ |
| 该微调还是用 RAG | 段7 0.3671 ❌ | **`### A3 · 微调与对齐`　0.4894** ✅ |
| 为什么不要学高数 | 段1 0.3582 ❌ | **`### A1 · 数学与工具直觉`　0.4007** ✅ |

**回归**：「每天要学几个小时？」在 23 段下 → `## 一、总周期与节奏` **0.3766 冲到 top-1**（旧切分第 6 名）；「红烧肉怎么做」→ 0.1407，**水位纹丝不动**。

**⑤ 两组对比数据（今天最值钱的产出）**

| | 旧切分 | 新切分 |
|---|---|---|
| 段数 | 10 | **23** |
| 最长段 | 1730 | **460** |
| 最短段 | 88 | **28** |
| 极差比 | 19.7× | **16.4×** ⚠️ **几乎没改善** |
| 无关问题 top-1 | 0.1474 | 0.1407 |
| 相关问题 top-1 | 0.3697 | **0.4915** |
| **水位差** | 0.22 | **0.35** ← 区分度反而变大 |

> ⚠️ **切细解决了"太长"，同时制造了"太短"。** 最长段从 1730 降到 460（好），但最短段从 88 掉到 28（坏）—— 比值只从 19.7 挪到 16.4。**"越细越好"是错的**，切分永远在"一段讲太多"和"一段讲太少"之间找平衡。

**认知（今天最值钱的部分）**

- **切分不改变任何信息**（总字数一字没变），它改变的只是**「一个向量要代表几件事」**。KV Cache 从"1730 字里的 20 字配角"变成"整段的主角"，分数涨了 **0.23（涨得最猛）** —— **被稀释得越狠的，切细后回报越大。**
- **高分 ≠ 答对。** 坏问题 top-1 = **0.3994**，好问题 top-1 = **0.3697** —— **答错的分数比答对的还高**。问题越笼统越贴近"整篇文档的主题"，分数越漂亮，答案越可能错。
- **分数反映的是「话题热度」，不是「答案位置」。** 段 3 那三个实验的分数（0.32~0.37）跟"答对那次"完全同水位，但答案全错。**阈值只能粗筛掉"红烧肉"那种 0.13，不能告诉你"找到了"。**
- **检索不是"找最像的"，是"找最能回答的"** —— 两者的分数尺度不一样。
- **根因在「查询侧」，不在「文档侧」**（段 2 只有 151 字，照样能当 top-1）。⚠️ 但**现实中问题不是你写的、是用户提的** —— 所以解法不是"以后把问题写好"，而是**在中间加一道 query 改写**。这是真实 RAG 系统的标准工序。
- 🔥 **同一段、同一个向量：问「KV Cache」得 0.4915，问「红烧肉」得 0.1401 —— 差 3.5 倍。** embedding 确实在算语义，不是随机发分数。
- **单位向量 → 余弦退化成点积**（用 `np.linalg.norm` 量出 `1.0000000214872777`）。但**不省那一步**：这是智谱**没写进文档的实现细节，不是承诺**。哪天表示方式变了，排序可能还对，但"分数 > 0.7 才算相关"这类判断会**静默失效**。真想省就显式自己归一化，让"归一化"这个动作看得见。
- **embedding 是"保留相似"，哈希是"打散相似"** —— 正好相反。这是它能拿来做检索的根本。

**Python 学习点**

- 🔴 **`def` 是运行时赋值语句 → 定义必须在调用之前**（否则 `NameError`）。Java 类方法顺序无关（类加载时全进方法表）—— **这是第一个"Java 思维直接套过来会翻车"的地方**。
- **`np.array()` 不是"把文字变数字"** —— 那些数从 JSON 解析出来那一刻就是 `float` 了。它是**换装**（`List<Double>` → `double[]`）：让"能存的"变成"能算的"。
- **`r.json()` ≈ `objectMapper.readTree()`，不是 `readValue`** —— 它只管把 JSON 变成 Python 对象树，**不关心结构**，所以每一层都得自己敲对键。
- **文字归 `list`，数字才归 `ndarray`** —— 两者分工不重叠。numpy 是终点站，不是起点。
- **`a * b` 是逐元素乘，`a @ b` 才是点积** —— numpy 头号坑。
- **`np.argsort` 返回的是下标、而且是升序** —— 它排分数、给你位置，因为你要拿下标回去取原文。
- **`np.linalg.norm(mat, axis=1)`** 才是"每行各算一次"；不传 `axis` 算的是整个矩阵（一个数）。Java 对照：`stream.mapToDouble(...)`（逐元素）vs `reduce`（整体）。
- **`v.shape` 是属性，不加括号**（`v.shape()` → `TypeError: 'tuple' object is not callable`）。`(2048,)` 里的逗号**不是笔误** —— 单元素元组。
- **列表推导 = Java 的"造容器 + for + add"三件事压成一行**：`[res["embedding"] for res in x]` —— `for` 后面是**来源**，最前面是**拿什么**，`return` 的是**新造的箱子**。
- **函数签名是契约**：`embed()` 收 `list[str]`、返 `list[list[float]]` —— 不能因为只查一个问题就让它返回单个向量。
- **「接住变量」的判据**：看它**能不能被用第二次**。只用一次（只为 `return`）可以内联；是观察窗口（能 `print` 一眼）就留着。
- 🔑 **Java 的 `String.split` 吃正则，Python 的 `str.split` 只吃字面量** —— 同名不同义。要正则必须显式走 `re.split`。
- **`(?=...)` 先行断言：只"检查后面有没有"，不吃掉** —— 所以切完标题原样留在段首。**切分时保留标题不是为了好看，是为了给后续步骤（标题路径）留出能力。**

**卡在哪（今天的坑）**

| 坑 | 病因 |
|---|---|
| `f"Bearer{key}"` 少一个空格 → 401 / code 1000 | 空格是**分隔符**不是装饰。服务端只能报"认证失败"—— **报错指向症状，不指向病因**（跟 Day 2 的 422 正相反：422 会告诉你缺哪个参数） |
| `x = r["data"]` → `TypeError: 'Response' object is not subscriptable` | `r` 是 Response 对象不是 dict，必须先 `r.json()`。`r` 和 `d` **只差一个字母** —— **单字母变量名是事故温床** |
| `np.array(dict)` 造出 **0 维 object 数组** | `d["data"]` 里每项是 dict，向量在 `["embedding"]` 字段里。少剥一层**不报错**，只是 `mat.shape` 变成 `(10,)`、dtype 变 `object` |
| `encoding` 拼成 `ecoding` | 关键字参数拼错**要到调用时才报** —— 所以「顺序错」和「拼写错」不会同时暴露，**一次只剥一层**。别把"修完还报错"当成"我没修对" |

**遗留（全部留档，先不动）**

- ⬜ **空壳段**：段 11 = `## 三、B 线`（**28 字**，子标题被切走后父标题成了孤儿）、段 14 = `### B3 · 检索与记忆`（76 字，正文只有"详见该目录的 `题目.md`"—— 它在实验 1 里拿过 0.3005 排第 2）。**解法正是 Day 3 遗留 #2「标题路径」**（给每段贴上"我属于哪一章"）。现在 top-1 全对、没造成实际伤害，按"看清症状再动手"留档。
- ⬜ **短段合并** —— 跟"切细"是一对反向的力，28 字那段的另一头。
- ⬜ **query 改写** —— 笼统问题失败的正解，等 B3 做 `research-assistant` 时补。
- ⬜ 打印仍是升序（`order[-3:]`）→ **最高分打在最后一行**，读着别扭。可换 `order[::-1][:3]`。
- ⬜ Day 3 打卡里那句 `re.split(r"\n#+ ", text)` 是**会把 `#` 和空格一起吃掉**的写法（已在 Day 4 改正为带先行断言的版本）—— 段首会变成"缺了井号的标题"，跟 `split("##")` 是同一个毛病。

### Day 5 · 09-23

**产出**：`python/rag.py`（新建）+ `python/day5.py` —— 问题进 → 检索 top-3 → 拼进提示 → 模型照资料回答并标出引用编号；`day4.py` 瘦身成一个 12 行的调用方。

**走到了链路最后一段**：

```
Day 1-2 会说话  →  Day 3 有资料  →  Day 4 会检索  →  Day 5 会照着资料回答（今天）
```

**① 今天第一个真正的工程动作：抽出 `rag.py`**

判据第三次升维 —— 从「变量」到「函数」：

| 时间 | 判据 | 结论 |
|---|---|---|
| Day 4 | **变量**：后面还会被用到吗？ | `chunks` 必须接住，`ans`（只为 return）可以省 |
| Day 5 | **函数**：有没有第二个使用者？ | `split_chunks` / `embed` / **`retrieve`** 三个都有（day5 要用）→ 抽 `rag.py` |

⚠️ 关键是**检索那 4 行也得抽**。不抽，day5 里就得重写一份 —— **同一段逻辑存两份 = 埋一个必然分叉的雷**：改了一处忘了另一处，**不报错，只是两个脚本开始给出不同答案**。

> **重构的验收标准是「行为一字不变」，不是「能跑就行」。** 手上正好有 Day 4 的旧输出，这是白送的安全网。

**② 抓到一个错，一层配置 + 一层语义**

| 坑 | 病因 |
|---|---|
| `CHAT_URL=https://api.openai.com/v1/chat/completions`<br>`os.environ['OPENAI_API_KEY']` | **平台混搭**：把智谱的模型名 `glm-4.7` 填进了 OpenAI 的地址。`rag.py` 里明明是对的（`open.bigmodel.cn` + `ZHIPU_API_KEY`）—— **新建文件时凭记忆重打了一遍**。同一件事写两份 → 必然分叉 |
| 现象：`KeyError: 'OPENAI_API_KEY'` | **前面 prompt 全部打印正常，最后一行才炸** —— 跟 Day 1「URL 凭记忆填错」是完全同一种形态 |

这就是「**平台 → 端点 → 模型**」三层模型的活教材：路径归端点管、模型名归 body 管，**各回各家**。

**③ 两问对照数据（今天的核心产出）**

| | 第一问「KV Cache 是什么？」 | 第二问「红烧肉怎么做？」 |
|---|---|---|
| 检索 top-3 | `### A4`（0.4915）+ B3 空壳段 + `### A2` | 三段 0.13~0.14 的无关段 |
| 模型回答 | 「KV Cache 对应后端经验中的缓存设计，涉及空间换时间、命中率、失效策略。[1]」 | 「资料里没有」 |
| 判定 | ⚠️ **看着对，其实不对** | ✅ **完美兜底** |

**④ 最值钱的发现：「提到了但没答」**

模型**一个字都没编**：引用 `[1]` 对得上、`[1]` 里确实有那句话、那句话本身也没错。

**问题在资料里。** 翻 prompt，`### A4` 提到 KV Cache 只有两处：

| 原文 | 这是什么 |
|---|---|
| `\| 为什么慢 \| KV Cache 是什么？批处理为什么能提速？ \|` | **待办清单** —— 在布置作业，不是解释 |
| `\| KV Cache \| 缓存设计：空间换时间、命中率、失效策略 \|` | **类比表的一格** —— 说它"像什么"，没说它"是什么" |

**《学习计划.md》里根本没有 KV Cache 的定义** —— 它是"计划"，不是"教材"。

> 🔑 **答案的形状由资料决定，不由问题决定。** 你问"是什么"，它递给你"像什么" —— 因为 A4 那段的主语是「和你后端经验的连接点」，它忠实搬运了资料的主语。

**⑤ 兜底条款的真实边界**

| | 红烧肉那问 | KV Cache 那问 |
|---|---|---|
| 资料里出现这个词吗 | ❌ 完全没有 | ✅ **有，出现两次** |
| 「资料里没有」触发了吗 | ✅ 触发了 | ❌ **没触发** |
| 模型干了什么 | 老实说没有 | **拿最接近的一句话顶上** |

> **那句「资料里没有就说没有」，只在「资料里完全没这个词」时生效。**

**为什么这次失败比幻觉难查得多：**

| | 幻觉 | 今天这种 |
|---|---|---|
| 说了资料外的话 | ✅ 是 | ❌ 没有，一字不越界 |
| 引用编号 | 可能编造 | ✅ 对得上 |
| 内容本身 | 可能错 | ✅ 甚至是对的 |
| 怎么发现 | 一眼看出来 | **得回原文核对"这个答案从哪来"** |

**四个"看起来都没错"，但你要的东西没拿到。**

**⑥ 责任分层（今天最值钱的一句话）**

| 环节 | 它的能力边界 |
|---|---|
| 检索 | 只保证"这段**跟问题相关**"，**永远不保证"这段能回答问题"** |
| 提示词 | 只挡得住"完全没提到"，挡不住"提到了但没解释" |
| 模型 | 忠实执行了指令 —— 它甚至没说错一个字 |
| **知识库** | 🔴 **根因：这份文档里没有 KV Cache 的定义** |

> **所以系统这次的行为，严格说是正确的。** 训练材料里没有答案时，把唯一沾边的一句话给你 —— 这就是一个诚实 RAG 系统该做的事。
>
> 这也直接预演了 B3 `research-assistant` 的真实难题：**「检索到相关文档」≠「文档里有答案」** —— 相似度、top-k 这些指标，**一个都测不出这件事**。

**Python 学习点**

- 🔴 **`import` 是个"动作"，不是"声明"** —— `from day4 import embed` 会把 `day4.py` **从上到下整份执行一遍**（连顶层那 20 多行流水账也跑 → 白调一次 API、白花一次钱）。Java 的 `import` 只让编译器知道有这个类，**方法体一行都不跑**。Java 唯一的近亲是 `static {}` 静态初始化块 —— **Python 里整个文件的顶层就是静态初始化块。**
- **`__name__ == "__main__"` 就是个普通的 `if`**，比的是一个普通字符串变量：直接跑 → `"__main__"`；被 import → `"day4"`。⚠️ 手滑写成 `"main"` **不报错，只是里面的东西永远不执行** —— 跟 `ecoding` / `r["data"]` / `Bearer` 少空格是同一族**静默失败**。
- **三引号字符串 ≈ Java text block，但有个致命差异**：Java **会自动剥掉公共缩进**，Python **一个字都不剥** —— 模板缩进着写，缩进会原样发给模型。**这就是 `PROMPT_TEMPLATE` 必须顶格放模块级的原因。**
- **`"\n".join(...)` ≈ `Collectors.joining("\n")`**；`enumerate(hits, start=1)` ≈ `IntStream.range(0, n)`，⚠️ 默认从 **0** 开始，编号要从 `[1]` 起就得写 `start=1`。
- **一个文件里的三层 = Java 一个 class 的三层**：模块级常量 ≈ `private static final` 字段、顶层 `def` ≈ `static` 方法、`if __name__` ≈ `main`。**Python 只是把 `class` 那个壳去掉了。**

**`build_prompt` 的四要素（为什么这么拼）**

| 元素 | 少了它会怎样 | Java 类比 |
|---|---|---|
| `[1][2][3]` 编号 | 拿到答案没法核对它是照资料说还是自己编 —— **引用是唯一的核验入口** | 日志的 traceId |
| `===== 资料 =====` 分隔 | 指令和资料糊在一起，模型分不清哪句是命令哪句是内容 | SQL 注入的雏形 |
| 「没有就说没有」 | 它默认为你服务，会拿自己的知识圆上去 | 接口没写兜底分支 |
| `要求` 放最后 | 模型对**末尾**指令的遵从度更高 | — |
| **分数不进 prompt** | 模型不懂 `0.4915` 意味着什么，写进去只会干扰它 | 分数留在终端给自己看 |

**卡在哪**

- ⚠️ **`CHAT_URL` / Key 名写错** —— 一处手滑引发两处修改，且报错要到**最后一行**才出现。
- ⚠️ **第一问的"通过"有水分** —— 验收标准「答对且引用对」**形式上满足、实质没满足**。**标准本身不够严**：应该再加一条「**它答的是不是资料里真有的东西**」。

**遗留（全部留档）**

- ⬜ **「提到了但没答」** —— 兜底指令的边界，只在"资料里完全没这个词"时生效。跟 Day 4 那三条并列（空壳段 / 短段合并 / query 改写）。
- ⬜ Day 6 四问里第 3 问（文档里完全没提的事 → 它编不编）**今天已经做了一半**（红烧肉 → 老实说没有）。
- ⬜ `.format()` 的坑：哪天模板里要出现 `{}` 字面量（比如要求模型输出 JSON）会报 `KeyError`，那时改 f-string 或写 `{{}}`。

**顺带记一笔（今天问到的「重排」）**：重排在计划里是**「了解级」**（`了解清单.md`「四、检索（RAG）」那道"为什么检索结果还要重排？"是**面试题**），执行表 Day 6 明确划入"今天别碰"。智谱有官方端点 `POST /api/paas/v4/rerank`（`model: rerank`、`documents` 最多 128 条、返回 `relevance_score` + `index`）。
> 🔑 **重排修的是「排序」，不修「资料里没有」** —— 所以它救不了今天这个失败。留到 B3 之后当小实验。

---

### Day 6 · 09-23

**产出**：一份 **10 条坑账本**（下面）+ 一次**被验证过的修复** + `python/day6.py`（四个切分边界 case）+ `python/day6_trace.py`（标题栈观察台）。`rag.py` 的 `split_chunks` 从 `re.split` 换成**逐行扫 + 标题栈**。

**四问的战果**

| # | 任务单要求 | 结果 |
|---|---|---|
| ① | 必须跨段才能答的问题 | 「B 线有哪几个项目」→ 答案段**全部落榜**，模型答"资料里没有"（**假的**） |
| ② | 特别长的文档 / 切分还成立吗 | 全是 `#` → 1 段；夹 `####` → 1 段。**正则把层数写死了** |
| ③ | 文档里完全没提的事 → 编不编 | 「推荐三本书」「推荐三本 RAG 的书」→ **两次都没编** ✅ |
| ④ | 同一个问题问三遍 → 稳不稳 | 检索**三次逐字一致**；生成只抖了引用编号 |

---

**① 边界：正则写死层数（坑①）**

`re.split(r"\n(?=#{2,3} )")` 只能切 2~3 级，`####` 完全切不动 → 整篇变 1 段。

改成 `#{2,}`：**救得了四级，救不了一级**。`#` 本来就不在切分范围里；要切它得写 `{1,}`，但那样 `# 学习计划` 会多切一刀，**23 → 24**，旧结果全失效。

---

**② 多跳：完整排名（今天最硬的产出）**

问题 `B 线有哪几个项目`，23 段全序（⚠️ **方括号里是下标，不是段号**：下标 = 段号 − 1）：

| 排名 | 下标 | 是哪一段 | 分数 |
|---|---|---|---|
| 1 | `[17]` | `### ✅ B 线入门标准` ← 正文里有「**三个项目**都能跑起来」 | 0.4624 |
| 2 | `[11]` | `## 三、B 线 · 开发工程线（实战级 · 70%）` ← **那个 28 字的空壳** | 0.4409 |
| 3 | `[22]` | `## 八、一句话记住方向` | 0.4281 |
| 4 | `[18]` | `## 四、排期表` | 0.4221 |
| 5 | `[16]` | `### B5` | 0.4192 |
| 6 | `[12]` | `### B1` | 0.4117 |
| 9 | `[13]` | `### B2` | 0.3871 |
| 14 | `[14]` | `### B3` | 0.3807 |
| 15 | `[15]` | `### B4` | 0.3781 |

> **A 线 5 段（下标 6~10）平均排 19.4 位；B 线 5 段（下标 12~16）平均排 9.8 位。**

🔑 **前 15 名里 B 线相关段占了 7 个；A 线段几乎全部沉底（A4 是第 23 名，最后一名）。** 问 B 线问题、A 线自动让位 —— 这是**修复起作用的直接证据**。

⚠️ **但答案段（B1–B5）一个都没进 top-3。** 三段 top-3 全都"跟 B 线有关"，可**没有一段列着项目清单**。所以这次「资料里没有」是**诚实的错**：喂进去的东西里确实没有答案。

**根因**：检索匹配的是「**措辞**」，不是「**答案的完整性**」。
B1–B5 五段内容很具体，但**一个字没写"项目"**；而入围的那段有「**三个项目**」这个短语 → 语义上"最像"问句 → 它赢了。
> 🔑 **"最像答案的那一段"，经常是"提到问题关键字的元信息段"，而不是"真正装着答案的内容段"。**

---

**③ 一次干净的因果实验（今天最漂亮的一组数据）**

同一份资料、同一个 B3 段，**只改问句措辞**：

| 问法 | B3 段分数 | B3 段排名 |
|---|---|---|
| `B 线有哪几个项目` | 0.3807 | **第 14 名** |
| `B3 · 检索与记忆 讲了什么` | **0.6044** | **第 1 名** |

**0.3807 → 0.6044，涨了 0.22。** 段本身**完全可召回**，落榜唯一的原因是**问法属于"聚合型"**（要 5 段的并集，而检索器一次只能给"单个段"）。

同一个招在 A4 上再验一次：

| 问法 | 结果 |
|---|---|
| `A4 讲了什么` | A4 段**落榜**（top-1 是 A2 段 0.3398，**top-3 全距仅 0.0046**）→ 答"资料里没有"（**假的**） |
| `A4 · 推理与部署 讲了什么` | A4 段 **0.4424，第 1 名** → 答对，引用 `[1] [3]` 都对得上 |

**根因：向量不辨数字。** `A2` 和 `A4` 共享同一个语义骨架（"A + 数字 + 主题"），数字在向量里的权重极低。

---

**④ 分数不能当判据（被自己的数据证伪两次）**

| 情形 | 分数 |
|---|---|
| 真命中（B3 段 / A4 段） | **0.6044** / 0.4424 |
| 高相关但**没答案**（A3 段 —— 正文里写着"什么时候该用 RAG"） | **0.4015** |
| 语义噪声地板 | 红烧肉 **0.1795** / 推荐三本书 **0.3331** |

**有答案 0.4424 ≈ 无答案 0.4015，只差 0.04。** "设一个拒答阈值"这条路**被自己的数据否掉了** —— 而且地板本身还在 0.18~0.33 之间飘。

> 🔑 **分数衡量的是"像不像"，不是"有没有答案"。这两件事在向量空间里本来就不同轴。** 想把它们分开，得换机制（重排 / 改写 / 多路），不是调一个数。

---

**⑤ 稳定性：检索 0 抖动，生成只抖在边角**

同一问题问三遍（`推荐三本 RAG 的书`）：

| 第几次 | 检索 top-3（分数/下标） | 答案 |
|---|---|---|
| 1 | 0.4015`[8]` / 0.3716`[19]` / 0.3324`[21]` | 资料里没有 **[1] [2] [3]** |
| 2 | **同上，逐字一致** | 资料里没有 **[1] [2]** |
| 3 | **同上，逐字一致** | 资料里没有 **[1] [2] [3]** |

- **检索层完全确定** → 不确定性 100% 来自生成。
- 正文 3/3 一致，**抖的只有引用编号** → `temperature=0` 的收益是**格式稳定**，不是"答案变对"。下游若用正则解析 `[n]` 做引用渲染，**这个抖动会直接让解析失败**。
- ⚠️ **测试问题选错了**：`资料里没有` 是个 4 个字的**退化答案**，模型几乎没有自由度，三次一致近乎必然。**要测抖动得用长答案的问题**（比如 A4 那问）。
- ⚠️ 第 2 次只标了 `[1] [2]`，可喂进去的是 3 段 —— 说明**模型并没有真在做"溯源"这个动作**，只是在服从"末尾带编号"这条格式指令。**引用编号 ≠ 证据。**

---

**⑥ 一次真实卡死：`requests.post` 没写 `timeout`**

现象：`=== Answer ===` 打印完之后**十分钟一片空白**，光标一直闪。

**诊断（这个推理可复用）**：Prompt 已经完整打印 ⇒ 切分、embed、检索、拼装**全部跑完了** ⇒ **卡点 100% 在 `ask()` 那一行的网络请求里**。

根因：`requests` **不传 `timeout` 就是永远等** —— 不是 30 秒，是无界。
> **Java 对照**：`HttpClient` 不调 `.timeout(...)`、`HttpURLConnection` 不调 `setReadTimeout()`，默认同样无界。**"每个外部调用都要有超时"是所有 HTTP 客户端的通用军规，不是 Python 技巧。**

后来它自己回来了 —— 说明那次是**慢**（模型思考久），不是死。**但没有 timeout 就等于没有防线。**

---

**⑦ 修复：标题路径（每次"改一处"，都要有前后对照）**

做法：`split_chunks` 从 `re.split` 换成**逐行扫 + 标题栈**，每段正文前拼上它的**祖先标题链**：

```
学习计划 · 双线并行 > 三、B 线 · 开发工程线（实战级 · 70%）
（这里原本是这一段自己的标题行：B1 · 最小可跑（1 周））
<原正文>
```

> ⚠️ **注意上面那行为什么写成括号说明**：如果照实写成"三个井号 + 空格 + B1"，**我自己的切分器会把它当成真标题** —— 这个坑当晚就撞上了，见下面「当场撞上的第 10 个坑」。

| 指标 | 修复前 | 修复后 |
|---|---|---|
| 段数（学习计划 / 执行表 / README） | 23 / 18 / 8 | **23 / 18 / 8（一字未变）**<br>⚠️ 执行表现在是 **19**（= 原 18 + 今天新写的 Day 6 打卡小节）；**过程中一度被自己的示例代码劈到 21** —— 见下面「当场撞上的第 10 个坑」 |
| 含「B 线」的段数（学习计划.md） | 9 / 23 | **14 / 23** |
| 新拿到「B 线」的是 | — | **下标 12~16 = B1~B5**（正文里一个字没写"B 线"，**是前缀带进来的**） |
| B 线问题 top-1 | 空壳段 | `✅ B 线入门标准` 0.4624 |
| 空壳段排名 | #1 | **#2**（0.4409） |
| B3 段（问 B3） | — | **#1（0.6044）** |

**⚠️ 修复有效，但不是免费的：**

| | A4 段 |
|---|---|
| 修复前 | **#1（0.4424）** |
| 修复后 | **#2（0.4294）**，被排期表段（0.4392）超过 |

原因：**加前缀 = 段变长 → 长段稀释。** 修的是"归属"，副作用恰好是"变长"。
> 好在答案没坏（A4 段仍在 top-3，模型答得完全正确）→ 算**轻微退化**，不是事故。

---

**🔑 一个根因，三次现形（今晚最该带走的一句）**

| 时间 | 现象 | 机制 |
|---|---|---|
| Day 4 | 正确答案（长段）排第 6 | **长段稀释** |
| Day 6 修复前 | 短空壳段（28 字）霸占 #1 | 短段关键词密度高 |
| Day 6 修复后 | A4（变长）丢 #1；B1–B5（长）仍打不过短段 | **同一机制，反向加剧** |

**「长段稀释」是贯穿始终的那个根因。** 标题路径修的是"归属"，不是"长度失衡"，所以它救不了这个 —— **"空壳/元信息段霸榜"这个现象一个字都没解决**（只是 #1 从空壳换成了另一个元信息段）。

> 🔑 **这正好是 rerank 存在的理由**：bi-encoder 把整段**压成一个固定向量**，长段的信号被平均掉；cross-encoder **不压缩**，逐 token 交叉注意力。
> **这个理由是今晚用实验推出来的，不是看视频看来的。**

---

**🔴 当场撞上的第 10 个坑：切分器不认识代码围栏**

写今天的打卡详情时，我在文档里贴了**两段**示例代码。两段里的第二行，都是**"三个井号 + 空格 + 一个标题名"**（当时是照实写出来的）。

再去跑 `split_chunks('第一周-执行表.md')` —— **18 段变成了 21 段**。那两行被当成**真标题**，把打卡详情当场劈成三截：

```
[16] len=3702  ## Day 6 打卡小节         ← 合法：今天新加的那一个标题
[17] len=1181  ### B1 · 最小可跑（1 周）  ← 伪标题，第一刀
[18] len=3311  ### B1 · 最小可跑（1 周）  ← 伪标题，第二刀
```

**"每贴一次示例，段数就涨一次"** —— 这个坑**不是我在实验里设计出来的，是自己撞上来的，而且当场撞了两次**。这比任何编造的案例都值钱：**真实数据里什么格式都有，而你的切分器不会先问你"这段是代码还是正文"。**

**根因**：现在的实现是**逐行正则匹配** `^(#{1,6}) +(.*)$`，**它不知道自己在不在代码块里**。
`学习计划.md`（23 段）和 `README.md`（8 段）没受影响 —— 只有执行表里出现了"代码块内以 `#` 开头的行"。

> 🔑 **两件事值得记**：
> 1. **"改文档"和"改代码"一样会改数据。** 这份执行表本身就在语料里（是回归测试的三份文档之一），往它里面塞代码块 = 改变输入。
> 2. **这个坑是自己送上门来的，不是在实验里"设计"出来的。** 而它恰恰是最真实的一类 —— **线上数据里什么格式都有**。
>
> 修法很小（`in_fence` 开关：遇到围栏就翻转，翻转期间不认标题），但**今天不修** —— 它改的是同一个 `split_chunks`，刚建好的基线会跟着变。留作明天第一个动作。
>
> ⚠️ **对账**：本页现在写成 **19 段**（把那两处字面标题改掉后回落 = 原 18 + 今天新增的 Day 6 打卡小节 1 个）。**过程中实测一度到 21。** 记在这里，防止明天对不上账。

**坑账本（Day 6 的正式产出）**

| # | 怎么坏的 | 为什么 | 怎么修 |
|---|---|---|---|
| ① | `####` 切不动，整篇 1 段 | 正则把层数写死 `{2,3}` | ✅ 改 `#{2,}` |
| ②a 归属缺失 | B1 段不知道自己属于"B 线" | 切分只留了段自己，丢了"来路" | ✅ **标题路径**（A/B 线分离开：9.8 vs 19.4 位） |
| ②b 短段霸榜 | 空壳段 / 元信息段永远在上 | **长段稀释** | ❌ 标题路径救不了 → **rerank** |
| ②c 聚合型多跳 | 答案 = 5 段的并集 | 召回单元 = 单段 | ❌ → **query 分解 / 多路检索** |
| ③ 编号无区分度 | 问 A4 捞回 A2，答"资料里没有"（假的） | 向量不辨数字 | ✅ **已证因果**：扩写问句 0.3398 → 0.4424 |
| ④ 分数不可作判据 | 有答案 0.4424 ≈ 无答案 0.4015 | "像" ≠ "有" | ❌ 阈值不可行（地板在 0.18~0.33 飘） |
| ⑤ 兜底靠提示词 | 检索对"有没有答案"完全无感 | 检索没有拒答能力 | ⬜ **待验**（把提示词那条删掉再问一次） |
| ⑥ 装饰件抖动 | 引用编号 `[1][2][3]` ↔ `[1][2]` | 采样随机性 + 格式指令 | `temperature=0` |
| ⑦ 网络无界 | `=== Answer ===` 后十分钟空白 | `requests.post` 无 `timeout` | `timeout=60` |
| ⑧ 环境绑定 | `day5.py` 只能在项目根目录跑 | 相对路径绑 cwd | 用 `__file__` 算绝对路径（加固，不急） |
| ⑨ 代码块里的 `#` | 贴了两段示例代码 → 执行表 **18 段被劈到 21 段** | 逐行正则**不知道自己在不在代码块里** | ⬜ `in_fence` 开关（明天第一个动作） |

**Python 学习点**

- 🔴 **闭包 + 赋值 = 局部变量（今晚最容易写错的一行）**：`seal()` 要清空外层的 `buf` ——
  `buf.clear()` ✅ 原地改对象；`buf = []` ❌ **会在函数内新建一个局部 `buf`**，外面那个纹丝不动 → 每段都会把前面所有内容再拼一遍。
  > **Java 对照**：Java 的 lambda / 匿名内部类**根本不能改外部局部变量**（必须 `final`）。Python 宽松，但判据很具体：**函数里只要出现对某名字的"赋值"，这个名字在整个函数内就是局部变量。** `buf.clear()` 是调用方法，不算赋值。
- **下标 ≠ 段号**：`[17]` 是**下标**，对应**第 18 段**。今晚为这个 off-by-one 白跑一轮。
  > 规律：**一律用下标**（代码给你的就是下标），要换算成"第几段"就 +1。
- **`while stack and stack[-1][0] >= level`**：`stack and` 必须在前面（短路），否则空栈取 `[-1]` 直接炸。Java 的 `!isEmpty() &&` 同理，顺序同样是硬要求。
- **`stack[-1][0]`**：栈里存的是 `(level, title)` **元组** —— 元组用整数下标取值，跟 `dict` 用键、`list` 用下标是三个不同的层。
- **相对路径绑 cwd**：`day5.py` 里写 `split_chunks("学习计划.md")` → **只能在项目根目录跑**；一 `cd python` 立刻 `FileNotFoundError`。
  > Java 对照：`new File("config.yml")` 同理，换个启动目录就炸。彻底修要用 `os.path.dirname(os.path.dirname(os.path.abspath(__file__)))`。

**卡在哪**

- ⚠️ **改代码之前没存基线** —— 修复前的完整排名没落盘（只在终端 scrollback 里），所以"好了多少"只能靠事后反推。
  > 🔑 **没有基线，就没有因果。** 和坑④是同一个家族。
- ⚠️ **修复不是免费的** —— 加前缀在帮"关键词"的同时**加剧了"长度失衡"**，A4 丢掉了 #1。**任何修复都要跑负样本回归**（今天跑了：红烧肉仍答"资料里没有"、A4 答案仍正确）。
- ⚠️ **"想要更详细的解释"不如"给一个能跑的观察台"** —— 标题栈这件事讲了两版（骨架 → 逐行表格）都没讲通，最后写了个能打印栈演化的脚本，一眼就懂了。
  > 记给自己：**卡住时先问"能不能让它跑起来看"。**
- ⚠️ **测试问题选错会浪费一整轮实验** —— 用 4 个字的退化答案测"稳定性"，测不出东西。

**遗留**

- ⬜ **坑⑨ 的修复**（给 `split_chunks` 加 `in_fence` 开关）—— **明天第一个动作**
- ⬜ **坑⑤ 的验证**（删掉提示词里的兜底要求，再问 `推荐三本 RAG 的书`）—— 今晚唯一没跑的那条
- ⬜ `timeout=60` + `temperature=0` 还没落地到代码
- ⬜ **rerank**（长段稀释的正解）/ **query 分解**（聚合型多跳的正解）/ **query 改写**（编号混淆的正解）—— 三条都只是"了解级"，Day 6 明令不碰，等 B3
- ⬜ Day 3 遗留的「短段合并」和 Day 4 遗留的「空壳段」，今天证明它们背后是**同一个根因**（长段稀释），先不单独动手

---

## 第二周预告（Transformer）

第一周把 Python 混熟之后再做，难度会砍半。原计划里砍掉的 MOE 不要了。

| 天 | 做什么 |
|---|---|
| 1 | 手撕一个最简单的神经网络训练（这时你 Python 熟了） |
| 2-3 | **用 Excel 手算 Transformer 的矩阵**——手算一遍比写代码理解更深 |
| 4 | Transformer 宏观理解（Encoder/Decoder、Multi-Head Attention） |
| 5-6 | 手撕 Multi-Head Attention、Transformer Block |
| 7 | 复盘 |

---

## 三条别忘的规矩

1. **每天只做当天那一格。** 提前做完就休息，不要抢第 2 天的内容。
2. **卡住先查，查不到再来问我。** 但别一卡就退回去系统地学——只查挡住你的那一个点。
3. **关窗口前先把产出贴进打卡区。** 判断标准就一句：关掉这个窗口，有什么东西留下来了？
