# 第一周 · 干中学执行表

> 用法：**每天只做当天那一格。** 做完把产出贴进打卡区，再关窗口。
> 唯一验收标准：**跑得出命令、看得见输出。** 看完教程说"我理解了"不算。

## 开工前（只做这两件）

- [ ] 装好 Python（3.10+），能跑 `python --version`
- [ ] 拿到一个大模型 API Key，能读进环境变量

搞不定就来找我。**不要在这一步顺手去看教程。**

---

## Day 1 · 让模型开口说话

**今天的产出**
一个 Python 脚本，跑起来打印出模型的一句话。

**动手做**
1. 用 pip 装一个 HTTP 客户端
2. 读环境变量里的 API Key
3. 发一次请求给模型接口
4. 把返回的文本打印出来

**卡住时才去查**：Python 怎么读环境变量 / 怎么发 HTTP 请求 / 返回的 JSON 长什么样

**今天别碰**：FastAPI · 异步 I/O · LangChain · 任何框架

**验收**：终端里能看到模型说的一句话。**看到就算过。**

**顺带搞懂（10 分钟，别多）**：Token 是什么、这一次请求大概花了多少钱

---

## Day 2 · 把它变成一个服务

**今天的产出**
一个网址，浏览器打开能跟模型对话。

**动手做**
1. 装 FastAPI
2. 把昨天的逻辑搬进一个接口里
3. 浏览器访问，把模型的话显示出来

**卡住时才去查**：FastAPI 最小路由怎么写 / 怎么接收请求里的参数

**今天别碰**：异步 I/O（同步够用）· Pydantic 校验 · Docker

**验收**：浏览器里能看到模型回答。

**顺带搞懂**：为什么要包成服务——因为后面前端、别人、你手机都要能调它。

---

## Day 3 · 给它一份自己的资料

**今天的产出**
把一份 md 文档切成一堆小段，把每一段打印出来看。

**动手做**
1. 找一份真实的文档（越长越好，比如你的一份笔记）
2. 读进内存
3. 按某种规则切成小段
4. 打印出段数 + 每段前 50 个字

**卡住时才去查**：为什么必须切（想想上下文窗口）/ 一段太短和太长分别坏在哪

**今天别碰**：各种 Loader（PDF / 网页 / Word 都先不管，只用 txt 或 md）· 任何向量库

**验收**：能看到段数和内容，**并且你能说出"切得算不算好"**。

---

## Day 4 · 把意思变成数字

**今天的产出**
输入一个问题，从段落堆里挑出最相关的 3 段。

**动手做**
1. 调一次 embedding 接口，把每段文字变成一串数字
2. 把问题也变成一串数字
3. 用 numpy 算它们之间的相似度
4. 排序，取前 3 段，打印出来

**卡住时才去查**：文字凭什么能变成数字、那串数字代表什么 / 余弦相似度怎么算

**今天别碰**：FAISS / Chroma —— **先用 numpy 手算。** 你只有几十段，用不着库。

**验收**：你问一个问题，它挑出来的 3 段，你自己看觉得**确实相关**。

**顺带搞懂**：向量库不是"让检索更强"，它是"数据大到内存放不下时要建索引"。

---

## Day 5 · 串成一个完整的 RAG

**今天的产出**
问题进 → 答案出，**并附上它引用的原文**。

**动手做**
1. 把 Day 3 + Day 4 拼起来
2. 把检索到的 3 段拼进给模型的提示里
3. 让模型基于这些资料回答，并要求它标出用了哪几段

**卡住时才去查**：提示词怎么拼 / 上下文为什么有限

**今天别碰**：LangChain（第二周再说）

**验收（两问都要过）**
- 问一个**文档里有**的问题 → 答对，且引用对
- 问一个**文档里没有**的问题 → 它应该老实说"资料里没有"

⚠️ **如果第二问它开始瞎编——记住那个瞬间。** 这就是"幻觉"，你亲手撞上了。

---

## Day 6 · 故意把它弄坏（本周最值钱的一天）

**今天的产出**
一份「我踩过的坑」记录，每条写清：怎么坏的 → 为什么 → 怎么修。

**要故意做的四件事**
1. 问一个**必须跨好几段**才能回答的问题 → 只取 top-3 会怎样？
2. 换一份**特别长**的文档 → 切分策略还成立吗？
3. 问一个**文档里完全没提**的事 → 它编不编？
4. **同一个问题问三遍** → 答案稳不稳？

**卡住时才去查**：为什么取 top-3 不够（想想多跳问题）/ 什么叫幻觉、为什么会有

**今天别碰**：重排、多路检索 —— **知道有这回事就够了**，别现在去实现。

**验收**：至少写清 3 个坑，且**每个坑你都能讲出原因**。

> 💡 这一天产出的东西，是你以后简历上最有价值的一块。别糊弄。

---

## Day 7 · 回头看，写下来

**今天的产出**
一页纸笔记，用**你自己的话**讲清：RAG 是什么、为什么每一环都要有。

**规则**
- 不写代码
- **不查资料**，硬写
- 写不出来卡在哪里，那就是你还没懂的地方，标出来

**今天别碰**：Docker 部署（第一周不需要）

**验收**：把这页读给一个完全不懂的人，他能听明白。

---

## 全周统一验收标准

> **跑得出命令、看得见输出，才算过。**

- 看懂教程 ≠ 学会了
- 照抄能跑 ≠ 学会了
- **能把它故意弄坏再修回来 = 学会了**

---

## 打卡区

| Day | 日期 | 产出跑通了吗 | 卡在哪 | 花了多久 |
|---|---|---|---|---|
| 1 | 09-20~09-22 | ☑ | 环境变量名拼成 APL / URL 靠记忆填 / 行尾 \ 续行符 / 缩进 / 没写 else 静默失败 / 老终端里还是旧 Key |
| 2 | 09-22 | ☑ | pip 下载断线（换清华镜像）/ 冒出 .venv 导致 requests 不在里面 / uvicorn 没 cd 到 python 目录 / URL 参数名与函数参数名不一致报 422 / 忘存盘改动不生效 / 答非所问（200 但输入被忽略） | 约 3.5h |
| 3 | 09-22 | ☑ | 路径读成了脚本自己（111 字符）/ `split("##")` 连带把 `###` 也切了 / `split` 吃掉分隔符导致标题残半 / 换 `"\n## "` 后又变成段太长、主题混在一起 | 约 1.5h |
| 4 | 09-22~09-23 | ☑ | `Bearer` 后少空格报 401（报错只说"失败"不说"少空格"）/ `r["data"]` 手滑（Response 不可下标）/ `np.array(dict)` 造出 object 数组 / `ecoding` 拼错 / `def` 写在调用之后 / **检索首次失败：答案排第 6** → 对照实验定位到"长段稀释" → 改切分修复 | 跨日（09-22 夜 ~ 09-23 上午） |
| 5 |  | ☐ |  |  |
| 6 |  | ☐ |  |  |
| 7 |  | ☐ |  |  |

### Day 1 · 09-20~09-22

**产出**：`python/day1.py` —— 终端里打印出模型的一句话。调的是智谱 `glm-4.7`。

两条最值钱的认知（比"会调 API"重要）：

- **「程序没报错」≠「跑通了」。** 没有 `else` 的 `if` 会让失败静默无声，把人往环境、网络、代理上引 —— 真正的原因被自己的代码藏起来了。
- **配置改了，已经跑着的进程不会自己变。** 环境变量在进程内存里，不在文件里；那个老终端一直拿着旧 Key。同一台机器上，两个进程读出了不同的值。

### Day 2 · 09-22

**产出**：`python/day2.py` —— `uvicorn day2:app --reload`，浏览器打开 `127.0.0.1:8000/xmy/?message=xxx` 能看到模型的回答。

- 形态：浏览器 → 我的服务 → 智谱。**这就是反向代理 / 网关的最小形态**（收请求 → 加工 → 转出去 → 拿回来 → 返回）。
- 用**查询参数**不用路径参数：路径里放不下 `/`、`?`，而"问题"里随时会有。
- 一律写 `def`，不用 `async def` —— 调的是阻塞的 `requests`，写 `async def` 会卡死事件循环。（对照：Spring MVC vs WebFlux，坑一模一样。）
- **逻辑一字没改**，变的只是「住在哪、被谁叫醒、结果交给谁」—— 这就是**控制反转**：Don't call us, we'll call you.

**今天最值钱的坑**：

- **「HTTP 200」≠「我的输入被处理了」。** 答非所问比报错难发现得多 —— 报错会大喊大叫，答非所问只是安静地回一句好听的废话。「怎么感觉不对」这个念头就是工程直觉。
- **跳过的验证步骤，会以"感觉不对"的形式回来找你。** 该分两轮跑的（先验参数、再接模型），合一轮做，就只能靠"感觉"。
- **422 的 `loc` 字段直接写着它要什么名字** —— 参数类错误先读 `loc`，别瞎猜。

**遗留**：⬜ 模型返回的 `\n` 在浏览器里显示成字面量（JSON 规范强制转义换行）。要换 `fastapi.responses` 的纯文本响应类型。不影响验收，可留到 Day 6。

### Day 3 · 09-22

**产出**：`python/day3.py` —— 把 `学习计划.md` 切成段，打印段数和每段前 50 字。

**两组对比数据（今天最值钱的产出）**：

| 切法 | 段数 | 段长 | 问题 |
|---|---|---|---|
| `split("##")` | **23 段** | 27 ~ 459 字（**17 倍**） | 标题残半、**段不自足** —— 正文跑到别的段去了 |
| `split("\n## ")` | **10 段** | 88 ~ 1730 字（**19.7 倍**） | 有大标题、自足性变好；但**长度悬殊反而变大**，最长那段 1730 字（占全文 1/3）一口气吞了 A1~A4 四个子节 |

⚠️ **注意第二行那个"反而"** —— 换切法时我的判断是"大部分能看懂"，但实测长度差距从 17 倍**涨到 19.7 倍**。**"感觉变好了"和"数据变好了"是两件事。**

> 为什么会看错：打印的是**每段前 50 字**，长度差异根本不在视野里。**输出里没有的信息，看不出问题。**

**认知**：

- **`split` 只做字符匹配，不看语义。** `"### "` 里天然含有 `"##"` —— 我以为在切二级标题，其实三级也被切了。**23 = 22 个 `##` + 1**，不是巧合。
- **`split` 会吃掉分隔符。** 所以每段开头都是"残破的标题"：H2 没了井号、H3 剩一个井号（看着反而像一级标题）。
- **切分没有"正确答案"，只有权衡。** 切小 → 精确但不自足（27 字那段只剩一句口号）；切大 → 自足但检索粗。**换一种切法，问题没消失，只是换了个形状。**
- **想按"任意级别的标题"切，`split` 做不到** —— "标题"是"1 个或多个 `#` + 空格"，那是**模式**不是**固定字符串**。得用正则（`re.split(r"\n#+ ", text)`）或逐行判断 `line.startswith("#")`。
  > ⚠️ **Day 4 修正**：`r"\n#+ "` 会把 `#` 连同空格**一起吃掉**（段首又变成"缺井号的标题"，跟 `split("##")` 同一个毛病）。正确写法要带**先行断言** → `re.split(r"\n(?=#{2,3} )", text)`。详见 Day 4 打卡详情。

**判断"切得好不好"的唯一方法**：随便挑一段，把其他段全蒙住 —— **只看它，能独立说明白一件事吗？**

**遗留**：⬜ 短段合并 / **标题路径**（把父级标题拼进段首，相当于段的 `traceId`）/ 相邻段重叠 —— 都等 Day 4 有了检索结果再决定修哪个方向。

### Day 4 · 09-22 夜 ~ 09-23

**产出**：`python/day4.py` —— 输入一个问题，从 23 段里挑出最相关的 3 段（embedding + numpy 手算余弦 + `argsort`）。

今天走的是一条**完整的工程曲线，不是顺风路**：

```
接口跑通 → 算出分数 → 检索失败（正确答案排第 6）→ 对照实验定位根因
   → 改切分 → 三个实验三中三 → 全量回归 → 打卡
```

**① 第一次检索：失败，但比成功值钱**

问「每天要花多少小时学 Agent」，正确答案（`## 一、总周期与节奏`，151 字，原文含"每天 3-4 小时"）排**第 6**，top-3 全是"文档元信息段"（导言 / 标题头 / "AgentGuide 怎么用"）。

- 「**学 Agent**」是**泛滥词** —— 几乎每段都有，连段 8「不通读 AgentGuide」都能拉分。它把查询向量拉向"整篇文档的主题"，而"文档主题"的典型代表正是标题和导言。
- 真正有区分度的「**每天几小时**」太短、太朴素，压不过它。
- 10 个分数全挤在 **0.2771 ~ 0.3994**，top-5 全距仅 **0.033** —— 正确答案与 top-1 的差距**就在噪声级别**。

**② 三组对照实验**

| 问题 | top-1 | 答案排第几 |
|---|---|---|
| 笼统版「每天要花多少小时学 Agent」 | 段1 **0.3994** | ❌ 第 **6**（0.3660） |
| 具体版「每天要学几个小时？」 | **段2 0.3697** | ✅ 第 1 |
| 「红烧肉怎么做」（完全无关） | 段6 0.1474 | — |

**③ 段 3 三战三败 → 「长段稀释」坐实**

段 3 有 **1730 字**，一口气吞了 `### A1 数学` `### A2 模型机制` `### A3 微调` `### A4 推理部署` 四个主题 —— 但**只有一个向量**。

| 实验 | 问题 | 答案藏在段3的哪块 | 段3排名 |
|---|---|---|---|
| 1 | KV Cache 是什么 | `### A4` | 第 2（0.2472） |
| 2 | 该微调还是用 RAG | `### A3` | **没进 top-3** |
| 3 | 为什么不要系统学高数 | `### A1` | **没进 top-3** |

**铁证在实验 2**：「什么时候该微调、什么时候该用 RAG？」是文档里 `### A3` 表格的**原文，一字不差** —— 照样输。**那 20 个字的信号，被同一个向量里另外 1710 个字的噪声压死了。**

**④ 改切分 → 三中三**

```python
raw = re.split(r"\n(?=#{2,3} )", text)      # 旧写法： text.split("\n## ")
```

| 问题 | 改前 | 改后 |
|---|---|---|
| KV Cache 是什么 | 段6 0.2575 ❌ | **`### A4 · 推理与部署`　0.4915** ✅ |
| 该微调还是用 RAG | 段7 0.3671 ❌ | **`### A3 · 微调与对齐`　0.4894** ✅ |
| 为什么不要学高数 | 段1 0.3582 ❌ | **`### A1 · 数学与工具直觉`　0.4007** ✅ |

**回归**：「每天要学几个小时？」在 23 段下 → `## 一、总周期与节奏` **0.3766 冲到 top-1**（旧切分第 6 名）；「红烧肉怎么做」→ 0.1407，**水位纹丝不动**。

**⑤ 两组对比数据（今天最值钱的产出）**

| | 旧切分 | 新切分 |
|---|---|---|
| 段数 | 10 | **23** |
| 最长段 | 1730 | **460** |
| 最短段 | 88 | **28** |
| 极差比 | 19.7× | **16.4×** ⚠️ **几乎没改善** |
| 无关问题 top-1 | 0.1474 | 0.1407 |
| 相关问题 top-1 | 0.3697 | **0.4915** |
| **水位差** | 0.22 | **0.35** ← 区分度反而变大 |

> ⚠️ **切细解决了"太长"，同时制造了"太短"。** 最长段从 1730 降到 460（好），但最短段从 88 掉到 28（坏）—— 比值只从 19.7 挪到 16.4。**"越细越好"是错的**，切分永远在"一段讲太多"和"一段讲太少"之间找平衡。

**认知（今天最值钱的部分）**

- **切分不改变任何信息**（总字数一字没变），它改变的只是**「一个向量要代表几件事」**。KV Cache 从"1730 字里的 20 字配角"变成"整段的主角"，分数涨了 **0.23（涨得最猛）** —— **被稀释得越狠的，切细后回报越大。**
- **高分 ≠ 答对。** 坏问题 top-1 = **0.3994**，好问题 top-1 = **0.3697** —— **答错的分数比答对的还高**。问题越笼统越贴近"整篇文档的主题"，分数越漂亮，答案越可能错。
- **分数反映的是「话题热度」，不是「答案位置」。** 段 3 那三个实验的分数（0.32~0.37）跟"答对那次"完全同水位，但答案全错。**阈值只能粗筛掉"红烧肉"那种 0.13，不能告诉你"找到了"。**
- **检索不是"找最像的"，是"找最能回答的"** —— 两者的分数尺度不一样。
- **根因在「查询侧」，不在「文档侧」**（段 2 只有 151 字，照样能当 top-1）。⚠️ 但**现实中问题不是你写的、是用户提的** —— 所以解法不是"以后把问题写好"，而是**在中间加一道 query 改写**。这是真实 RAG 系统的标准工序。
- 🔥 **同一段、同一个向量：问「KV Cache」得 0.4915，问「红烧肉」得 0.1401 —— 差 3.5 倍。** embedding 确实在算语义，不是随机发分数。
- **单位向量 → 余弦退化成点积**（用 `np.linalg.norm` 量出 `1.0000000214872777`）。但**不省那一步**：这是智谱**没写进文档的实现细节，不是承诺**。哪天表示方式变了，排序可能还对，但"分数 > 0.7 才算相关"这类判断会**静默失效**。真想省就显式自己归一化，让"归一化"这个动作看得见。
- **embedding 是"保留相似"，哈希是"打散相似"** —— 正好相反。这是它能拿来做检索的根本。

**Python 学习点**

- 🔴 **`def` 是运行时赋值语句 → 定义必须在调用之前**（否则 `NameError`）。Java 类方法顺序无关（类加载时全进方法表）—— **这是第一个"Java 思维直接套过来会翻车"的地方**。
- **`np.array()` 不是"把文字变数字"** —— 那些数从 JSON 解析出来那一刻就是 `float` 了。它是**换装**（`List<Double>` → `double[]`）：让"能存的"变成"能算的"。
- **`r.json()` ≈ `objectMapper.readTree()`，不是 `readValue`** —— 它只管把 JSON 变成 Python 对象树，**不关心结构**，所以每一层都得自己敲对键。
- **文字归 `list`，数字才归 `ndarray`** —— 两者分工不重叠。numpy 是终点站，不是起点。
- **`a * b` 是逐元素乘，`a @ b` 才是点积** —— numpy 头号坑。
- **`np.argsort` 返回的是下标、而且是升序** —— 它排分数、给你位置，因为你要拿下标回去取原文。
- **`np.linalg.norm(mat, axis=1)`** 才是"每行各算一次"；不传 `axis` 算的是整个矩阵（一个数）。Java 对照：`stream.mapToDouble(...)`（逐元素）vs `reduce`（整体）。
- **`v.shape` 是属性，不加括号**（`v.shape()` → `TypeError: 'tuple' object is not callable`）。`(2048,)` 里的逗号**不是笔误** —— 单元素元组。
- **列表推导 = Java 的"造容器 + for + add"三件事压成一行**：`[res["embedding"] for res in x]` —— `for` 后面是**来源**，最前面是**拿什么**，`return` 的是**新造的箱子**。
- **函数签名是契约**：`embed()` 收 `list[str]`、返 `list[list[float]]` —— 不能因为只查一个问题就让它返回单个向量。
- **「接住变量」的判据**：看它**能不能被用第二次**。只用一次（只为 `return`）可以内联；是观察窗口（能 `print` 一眼）就留着。
- 🔑 **Java 的 `String.split` 吃正则，Python 的 `str.split` 只吃字面量** —— 同名不同义。要正则必须显式走 `re.split`。
- **`(?=...)` 先行断言：只"检查后面有没有"，不吃掉** —— 所以切完标题原样留在段首。**切分时保留标题不是为了好看，是为了给后续步骤（标题路径）留出能力。**

**卡在哪（今天的坑）**

| 坑 | 病因 |
|---|---|
| `f"Bearer{key}"` 少一个空格 → 401 / code 1000 | 空格是**分隔符**不是装饰。服务端只能报"认证失败"—— **报错指向症状，不指向病因**（跟 Day 2 的 422 正相反：422 会告诉你缺哪个参数） |
| `x = r["data"]` → `TypeError: 'Response' object is not subscriptable` | `r` 是 Response 对象不是 dict，必须先 `r.json()`。`r` 和 `d` **只差一个字母** —— **单字母变量名是事故温床** |
| `np.array(dict)` 造出 **0 维 object 数组** | `d["data"]` 里每项是 dict，向量在 `["embedding"]` 字段里。少剥一层**不报错**，只是 `mat.shape` 变成 `(10,)`、dtype 变 `object` |
| `encoding` 拼成 `ecoding` | 关键字参数拼错**要到调用时才报** —— 所以「顺序错」和「拼写错」不会同时暴露，**一次只剥一层**。别把"修完还报错"当成"我没修对" |

**遗留（全部留档，先不动）**

- ⬜ **空壳段**：段 11 = `## 三、B 线`（**28 字**，子标题被切走后父标题成了孤儿）、段 14 = `### B3 · 检索与记忆`（76 字，正文只有"详见该目录的 `题目.md`"—— 它在实验 1 里拿过 0.3005 排第 2）。**解法正是 Day 3 遗留 #2「标题路径」**（给每段贴上"我属于哪一章"）。现在 top-1 全对、没造成实际伤害，按"看清症状再动手"留档。
- ⬜ **短段合并** —— 跟"切细"是一对反向的力，28 字那段的另一头。
- ⬜ **query 改写** —— 笼统问题失败的正解，等 B3 做 `research-assistant` 时补。
- ⬜ 打印仍是升序（`order[-3:]`）→ **最高分打在最后一行**，读着别扭。可换 `order[::-1][:3]`。
- ⬜ Day 3 打卡里那句 `re.split(r"\n#+ ", text)` 是**会把 `#` 和空格一起吃掉**的写法（已在 Day 4 改正为带先行断言的版本）—— 段首会变成"缺了井号的标题"，跟 `split("##")` 是同一个毛病。

---

## 第二周预告（Transformer）

第一周把 Python 混熟之后再做，难度会砍半。原计划里砍掉的 MOE 不要了。

| 天 | 做什么 |
|---|---|
| 1 | 手撕一个最简单的神经网络训练（这时你 Python 熟了） |
| 2-3 | **用 Excel 手算 Transformer 的矩阵**——手算一遍比写代码理解更深 |
| 4 | Transformer 宏观理解（Encoder/Decoder、Multi-Head Attention） |
| 5-6 | 手撕 Multi-Head Attention、Transformer Block |
| 7 | 复盘 |

---

## 三条别忘的规矩

1. **每天只做当天那一格。** 提前做完就休息，不要抢第 2 天的内容。
2. **卡住先查，查不到再来问我。** 但别一卡就退回去系统地学——只查挡住你的那一个点。
3. **关窗口前先把产出贴进打卡区。** 判断标准就一句：关掉这个窗口，有什么东西留下来了？
