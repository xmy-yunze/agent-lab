# agent-lab · 项目指令

每次在这个目录里开新会话，先读这份文件。

## 这个项目是干什么的

Agent 工程的学习载体。

目标不是「看完某个教程」，而是**产出一批能跑起来、能讲清楚的小系统**。

主线思路：后端能力向 Agent 工程延伸。会话管理、状态机、限流重试、链路追踪这些后端早就解决过的问题，在 Agent 场景里换了名字重新出现。这里就是把它一个个对应起来、亲手实现一遍的地方。

## 学习方式（重要，别搞反）

- **出题 - 解题**：先给我贴近实际的题目和解题思路，**代码我自己写**。不要直接甩完整实现。
- 讲知识点时：先讲原理 → 再讲怎么落到代码 → 最后给验证方式。我要的是「为什么这么设计」，不是「照抄这段代码」。
- **讲 Python 代码时，用 Java 作对照。** 我 Java 熟、Python 不熟——两边都摆出来，我理解得快。
- 卡住了可以给提示、给方向、指出我错在哪，但别替我写完。

## 代码约定

- **注释用中文，标识符用英文**。
- **语言分工（按阶段）**：
  - **当前阶段 · 第一周（热身 + RAG 认知线）→ Python 3.12**。单文件脚本为主，不建工程、不引框架。Python 的目标只到「能读懂能改」，不是重学编程。
  - **正式产出物（min-agent-loop / research-assistant / harness-demo）→ Java 17、Spring Boot 4.0.7、Gradle**。技术栈不变，第一周结束后回到这条线。
- **手写阶段不许引 Agent 框架**（Java 侧：Spring AI / LangChain4j；Python 侧：LangChain / LlamaIndex）——这个阶段就是要自己把循环写出来，看清里面是什么。
- 先跑通再交付。**没验证就明说没验证**，不要含糊过去。
- 引入任何依赖前，先确认与 Spring Boot 4.0.7 的版本兼容性，别照抄博客。

## 产出放哪（核心规矩）

- 一个产出物一个目录，代码、笔记、踩坑记录都放在该目录里。
- **每次学习结束，先把成果落盘，再关窗口。** 判断标准就一句：关掉这个窗口，有什么东西留下来了？
- 跨产出物的通用笔记放 `notes/`。
- 涉及简历、面试的内容放 `求职素材/`。

## 参考地图

学习方向参考 GitHub 上的 `adongwanai/AgentGuide`。

**但只当索引用，不要通读**——那个仓库上万字正文加上百个外链，通读必然烂尾。真正要做的事情已经抽出来写在 `README.md` 里了。

## 不管的事（避免跑偏）

- 不碰模型训练 / 微调 / RL（那是另一个专业方向，跟后端是两条路）。
- 不追框架用法大全。框架会用就行，重点是搞明白底下在发生什么。
